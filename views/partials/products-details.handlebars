<section class="card">
  <a href="/products/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">

    </div>
  </a>
</section>
